pattern to initiate new chat, since free tier chat limited in length.
---
task: transcode(source+test) custom battle tested math lib from ts code into zig code.
---
context: fastest calculation + smallest ram usage.
at least use zig's @vector + simd + pointers(objects to be filled/mutated by calculation/result of function, send by reference instead of value).

Optional implement slice to vectors wrapper, that has incoming slice but uses fastest vector implementation uses overloading f.e.: fn a(x:vector){fastest code}, fn a(x:slice){more user friendly call, but internally calls the fastest code}. Optional will be desided for each method personally.
---
origin: https://github.com/healingdrawing/gemm/tree/zig-concat-discovering/ts

includes ts lib code in "gemm.ts" file.

includes ts lib test in "tests" folder.

includes ts lib simple benchmarks in "bench" folder.
---
destination: https://github.com/healingdrawing/gemm/tree/zig-concat-discovering/zig

includes zig lib skeleton for transcode result.

includes lib future code "src" folder, with already implemented methods(start point for continue).

includes concat.sh script to collect methods into gemm.zig file before build *.a libs in folder zig-out/lib.

includes "test_libs.zig" file to test compiled *.a files. This file must be extended during transcoding for every method transcoded.

includes "test_libs.sh" file to execute test for compiled *.a files.

every method from gemm.ts in zig case placed in separate folder with same name for folder and file f.e.
ts method v3v3scalar() has zig pattern:
- src/v3v3scalar/v3v3scalar.zig
- src/v3v3scalar/test_v3v3scalar.zig

---
ai workflow steps:
- analyze the gemm.ts file, and collect list of the methods under 3d specific implementation marker "/* 3D specific below, for performant calcs */".
- only methods below this marker should be implemented
- create a list of available method names to implement
- analyze already implemented names from "gemm.zig" file
- offer to user to continue from one of the still not transcoded properly methods.
- always follow skeleton src/module/module.zig etc
- in response separated file stuff always must be marked with target file name
- transcode method from ts to zig respectively follow the skeleton (and follow the structure of the file f.e. v3v3scalar.zig where imports are on the top before concat marker, since they will be cut later)
- write test for transcoded to zig method follow test_module.zig pattern see test_v3v3scalar.zig etc
- after check and pass tests by user, write tests for generated *.a files, at this time use the tests for methods from "ts/tests" folder. They are implemented uses bun runtime, so continue if possible use bun tests approach(way) , to test *.a files

ok, looks like testing need refactoring, really to json approach or so. To provide both ts and zig-to-lib test uses one input data for tests.

oh shit. discovering of the wasm etc shows that it is like a very fast flying dog. but this dog is one leg, with cut groin and can fly only from top to down. So testing with one source of truth can be overhelming. parallel testing does not look nice, it is two test branchs. zig and ts separately. does not look good.