pattern to initiate new chat, since free tier chat limited in length.
---
task: transcode(source+test) custom battle tested math lib from ts code into zig code.
---
context: fastest calculation + smallest ram usage.
at least use zig's @vector + simd + pointers(objects to be filled/mutated by calculation/result of function, send by reference instead of value).

Optional implement slice to vectors wrapper, that has incoming slice but uses fastest vector implementation uses overloading f.e.: fn a(x:vector){fastest code}, fn a(x:slice){more user friendly call, but internally calls the fastest code}. Optional will be desided for each method personally.
---
origin:

https://github.com/healingdrawing/gemm/tree/zig-concat-discovering/ts/gemm.ts
battle tested typescript version of the math lib i develop

target:

https://github.com/healingdrawing/gemm/tree/zig-concat-discovering/ts/terminalcall.ts
used in test.zig to test ts vs zig version output

https://github.com/healingdrawing/gemm/tree/zig-concat-discovering/zig
skeleton for zig version
ONLY 3D SPECIFIC VERSIONS PLAN TO TRANSCODE TO ZIG FROM gemm.ts(there is comment)

https://github.com/healingdrawing/gemm/tree/zig-concat-discovering/zig/src/(method)/(method).zig
file for zig implementation of the method from gemm.ts . The "//-concat marker" is IMPORTANT.

https://github.com/healingdrawing/gemm/tree/zig-concat-discovering/zig/src/(method)/bench_(method).zig
benchmark file to test the fastest and/or most stable time method in noise environment(desktop with browser in tray/deploy onrender.com free tier)

https://github.com/healingdrawing/gemm/tree/zig-concat-discovering/zig/test.zig
manages zig tests

https://github.com/healingdrawing/gemm/tree/zig-concat-discovering/zig/tests/data_(method).zig
the input/output data for tests uses test.zig

https://github.com/healingdrawing/gemm/tree/zig-concat-discovering/zig/tests/test_(method).zig
the zig test for (method)

At the moment this skeleton works, with rare fix needed in custom case.
Always follow skeleton above.

AI TASK

For new chat always:

- discover repository https://github.com/healingdrawing/gemm/tree/zig-concat-discovering
- analyze 3d specific methods in gemm.ts
- analyze already implemented zig methods with the same name/folder name respectively
- offer/advice to me implement the next not implemented in zig method from gemm.ts file. Maybe i will choose what method next. Prefer focus the simpliest one first

For new method request, sequently, step by step:

1 - transcode/generate (method).zig file. Consider to add possible imlementation with fastest calculation approach, then stability also will be discovered in bench. i will modify/experiment with code manually also, based on generated by you versions.

2 - generate bench_(method).zig file. Benchmark will show what is what. use 20 000 000 iterations first. Later with advanced versions i will manually increase iterations up to 1 000 000 000, to decrease the noise impact.

3 - generate data_(method).zig . to sufficiently cover test cases for implemented method

4 - generate test_(method).zig .

ALWAYS FOLLOW SKELETON.
Skeleton is already tested and works, and looks like can be extended without big obstacles for common cases.

If you have any suggestions, shortly inform/ask me, but by default always try to follow the skeleton and context described above.

when analyze bench output, show me the TWO SEPARATED TABLES each sorted by positions with the full method name:
- first table sorted by the balanced fastest time of execution (sum of all times devided by number of the executions for the same method from each execution)
- second table sorted by the most stable executions time (max_time - min_time), f.e. (0.8 - 0.08) = 0.72 then sort the smallest on top as winner