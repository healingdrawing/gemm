# gemm
geometry multi-dimensional manipulations

## zig and typescript implementations
- **f32** focused.
- **1e-6** tolerance used to handle floating-point precision edge cases between zig and typescript.

## haxe implementation
**GeometryXD(2018) is Deprecated**, was used as base for zig and typescript, not developed anymore, based on very first python3 origin(2015+-) created from reading the paper books ;) then next steps.
